set remote hardware-breakpoint-limit 6
set remote hardware-watchpoint-limit 4

#monitor reset halt
#load

# (initCommands)
#set remotetimeout 20
#monitor reset init
#monitor sleep 200
#monitor halt 2000
#monitor sleep 200

# (runCommands)
#monitor sleep 200
#reset init
